# V6 部署

1. 解壓縮 ZIP。
2. 將所有檔案上傳到 GitHub `LiaoWei99/tw-stock-dashboard` 的 `main` 根目錄並覆蓋同名檔案。
3. Commit。
4. Render 部署後開啟正式網址。

V6 新增：個股精確搜尋後的隔日沖 100 分模型（基本20/技術40/籌碼40）、隔日沖不符合時自動轉波段 100 分模型（基本35/技術35/籌碼30）、資料完整度、缺失資料不計分、符合波段條件時才產生觀察持有時間/布局區/目標/失效價/RR。

注意：法人資料屬盤後官方資料；未取得足夠歷史K線或籌碼資料時不產生假精準的進出場價格。
