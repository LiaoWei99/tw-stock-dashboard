# V9.2.1 Safe Market Data Fix

- Numeric search does not fetch the full company master.
- Company master refresh is background-only; requests read memory only.
- Numeric codes query TSE + OTC MIS channels directly.
- Current price uses only MIS last-traded `z`; missing price stays Unavailable and never falls back to previous close.
- UI shows quote source/date/time.
- Heavy modules remain independent.
